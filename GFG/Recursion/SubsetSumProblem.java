// WAF that takes sum and array and give output the number of subsets it have that results the sum

public class SubsetSumProblem {

    public static int numOfSum(int arr[], int  ) {
        return numOfSum(arr, sum)
    }

    public static void main(String[] args) {

    }
}
